## 19. The interface review — alignment, reach and reliable evidence

The Table and Fixtures reports were two different CSS faults. Numeric headings lost a
specificity contest to the table header's left alignment; fixture div rows added padding
outside width:100%, while button rows happened to inherit border-box from the browser.
The reset and explicit numeric-cell alignment now make both contracts visible in the CSS.

The wider review found that a clean card was not necessarily a visible card. On a 390px
phone, the tactics grid's intrinsic minimum pushed the card 49px past the viewport. The
tracks and children can now shrink, and the pitch keeps its ratio inside the available
width. Compact shirts retain 56px pointer targets. A late illustration rule also put the
topbar and main into equal stacking contexts, letting main cover the fixed navigation;
the topbar now stays above the content.

The game's stated 56px target size now reaches compact buttons, range inputs, badge
controls, fields and navigation. Narrow phones use two labelled rows; tablets keep the
labelled bottom bar until 1080px instead of reducing it to icons. Fixtures no longer
inherit a clickable row hover. Bar accessible names include magnitude, and the mobile
squad's header/metric width shares one token.

The review also repaired its own instrument. The old contact sheet accumulated careers
between widths and mislabeled several Home screens as intro or match frames. The screen
harness now uses a fixed seed, resets isolated storage, asserts the expected surface,
captures all three letter states and records measurements. It checks card and viewport
bounds, numeric headers, control sizes and navigation hit testing, with explicit room for
intentional scrollers and modal blocking. A content-box negative control must fail.

The dusk palette, per-career accent, system stack and procedural illustrations remain the
visual authority. Sale undo and further formation-preset simplification are recorded as
owner decisions, not silently added to a layout pass. The audit and portable HTML canvas
live in games/touchline/docs/design-review/; the canvas substitutes for unavailable Claude
Design tooling and embeds review screenshots without adding production asset files.
